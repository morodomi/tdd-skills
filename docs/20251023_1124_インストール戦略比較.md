# インストール戦略の比較

**作成日**: 2025-10-22
**目的**: 複数プロジェクトへのSkills導入方法を決定する

---

## 1. Claude Skillsのインストール場所

### 1.1 3つのインストール先

```
1. Personal/Global Skills
   ~/.claude/skills/
   ↓
   すべてのプロジェクトで利用可能
   個人用、実験的なSkills

2. Project Skills
   your-project/.claude/skills/
   ↓
   プロジェクト固有
   gitでチーム共有可能

3. Plugin Skills
   プラグイン経由
   ↓
   プラグインインストール時に自動利用可能
```

### 1.2 優先順位（重要）

**同名のSkillsが複数の場所に存在する場合**:

```
プロジェクトレベル (.claude/skills/)
    ↓ 優先
グローバルレベル (~/.claude/skills/)
    ↓ 優先
プラグインレベル
```

**根拠**:
- Claude Codeの他の機能（subagents, MCP, settings）と同じパターン
- より具体的（プロジェクト）な設定が、より広範（グローバル）な設定を上書き

**つまり**:
```
~/.claude/skills/tdd-plan/  （グローバル）
  +
project/.claude/skills/tdd-plan/  （プロジェクト固有）
  ↓
プロジェクト固有が優先される
```

---

## 2. 3つのインストール戦略

### 戦略A: 完全グローバル

```
~/.claude/
└── skills/
    ├── tdd-init/
    ├── tdd-plan/
    ├── tdd-red/
    ├── tdd-green/
    ├── tdd-refactor/
    └── tdd-review/

your-laravel-project/
└── (Skillsなし)

your-django-project/
└── (Skillsなし)
```

#### メリット
- ✅ **1回のインストールで全プロジェクトに適用**
- ✅ メンテナンスが楽（1箇所を更新すれば全プロジェクトに反映）
- ✅ プロジェクト構成がシンプル

#### デメリット
- ❌ **フレームワーク固有の調整が難しい**（Laravel/Django/Flask/WordPress）
- ❌ プロジェクト固有のルールを適用できない
- ❌ gitでチーム共有できない（個人専用）
- ❌ 既存/新規プロジェクトの違いに対応しにくい

#### 適用場面
- すべてのプロジェクトが同じフレームワーク
- プロジェクト間の差異が小さい
- 完全に個人開発のみ

---

### 戦略B: 完全プロジェクト固有

```
~/.claude/
└── (Skillsなし)

your-laravel-project/
└── .claude/
    └── skills/
        ├── tdd-init/
        ├── tdd-plan/
        └── ...

your-django-project/
└── .claude/
    └── skills/
        ├── tdd-init/
        ├── tdd-plan/
        └── ...
```

#### メリット
- ✅ **フレームワークごとにカスタマイズ可能**
- ✅ プロジェクト固有のルールを適用できる
- ✅ gitでチーム共有できる
- ✅ 既存/新規の違いに対応できる

#### デメリット
- ❌ **各プロジェクトにインストールが必要**
- ❌ メンテナンスコスト高（全プロジェクトを更新）
- ❌ 共通部分の重複

#### 適用場面
- フレームワークが多様（Laravel/Django/Flask/WordPress）
- プロジェクトごとに要件が異なる
- チーム開発の可能性がある

---

### 戦略C: ハイブリッド（グローバル + プロジェクト上書き）

```
~/.claude/
└── skills/
    ├── tdd-init/          # 汎用版
    ├── tdd-plan/          # 汎用版
    ├── tdd-red/           # 汎用版
    └── ...

your-laravel-project/
└── .claude/
    └── skills/
        ├── tdd-plan/      # Laravel固有版（上書き）
        └── laravel-quality/  # Laravel専用

your-django-project/
└── .claude/
    └── skills/
        ├── tdd-plan/      # Django固有版（上書き）
        └── django-quality/  # Django専用
```

#### メリット
- ✅ **共通部分を再利用**
- ✅ **フレームワーク固有の調整も可能**
- ✅ 柔軟性が最も高い
- ✅ プロジェクト数が増えても管理可能

#### デメリット
- ❌ **管理が複雑**（どこに何があるか把握が必要）
- ❌ 優先順位の理解が必要
- ❌ デバッグが難しい（どのSkillが使われているか不明瞭）

#### 適用場面
- フレームワークが多様だが共通部分も多い
- 段階的に導入したい
- 実験的な機能とプロダクション機能を分けたい

---

## 3. あなたのケースでの推奨

### あなたの状況
- ✅ 個人開発
- ✅ 複数プロジェクト（Laravel/Django/Flask/WordPress）
- ✅ 既存プロジェクト + 新規プロジェクト
- ✅ 最終的にすべてのプロジェクトに導入したい

### 推奨: **戦略B（完全プロジェクト固有） + テンプレートリポジトリ**

#### 理由

1. **フレームワークの多様性**
   ```
   Laravel  → PHPUnit/Pest, PHPStan, Laravel Pint
   Django   → pytest, mypy, black
   Flask    → pytest, mypy, black
   WordPress → PHPUnit, WordPress Coding Standards
   ```
   → フレームワークごとにツール・規約が異なるため、プロジェクト固有が必須

2. **既存 vs 新規の違い**
   ```
   既存プロジェクト → 既存のテストパターンに適応
   新規プロジェクト → 厳格なTDDルールを適用
   ```
   → プロジェクトごとのカスタマイズが必要

3. **将来のチーム化**
   - プロジェクト固有ならgitで共有可能
   - グローバルは個人専用で共有不可

4. **メンテナンスの現実性**
   - テンプレートリポジトリから配布
   - 各プロジェクトは独立して進化可能
   - 必要なプロジェクトだけ最新版に更新

---

## 4. 推奨実装アーキテクチャ

### 4.1 テンプレートリポジトリ構成

```
ClaudeSkills/  (このリポジトリ)
├── templates/
│   ├── laravel/
│   │   ├── .claude/
│   │   │   ├── skills/
│   │   │   │   ├── tdd-init/
│   │   │   │   │   └── SKILL.md
│   │   │   │   ├── tdd-plan/
│   │   │   │   │   ├── SKILL.md
│   │   │   │   │   └── templates/
│   │   │   │   │       └── PLAN.md.template
│   │   │   │   ├── tdd-red/
│   │   │   │   ├── tdd-green/
│   │   │   │   ├── tdd-refactor/
│   │   │   │   ├── tdd-review/
│   │   │   │   └── laravel-quality/
│   │   │   │       ├── SKILL.md
│   │   │   │       └── scripts/
│   │   │   │           ├── phpstan.sh
│   │   │   │           └── pest.sh
│   │   │   └── commands/
│   │   │       ├── tdd-init.md
│   │   │       ├── tdd-plan.md
│   │   │       └── ...
│   │   ├── CLAUDE.md.template
│   │   └── install.sh
│   │
│   ├── django/
│   │   ├── .claude/
│   │   │   ├── skills/
│   │   │   │   ├── tdd-init/
│   │   │   │   ├── tdd-plan/
│   │   │   │   └── django-quality/
│   │   │   │       └── scripts/
│   │   │   │           ├── pytest.sh
│   │   │   │           └── mypy.sh
│   │   │   └── commands/
│   │   ├── CLAUDE.md.template
│   │   └── install.sh
│   │
│   ├── flask/
│   └── wordpress/
│
├── docs/
├── examples/
└── README.md
```

### 4.2 インストール方法

#### Option 1: 直接クローン（推奨）

```bash
cd your-laravel-project

# テンプレートを取得
git clone https://github.com/yourname/ClaudeSkills .tdd-setup
cd .tdd-setup/templates/laravel
./install.sh ../../..
cd ../../..
rm -rf .tdd-setup
```

#### Option 2: curlでダウンロード

```bash
cd your-laravel-project

# インストールスクリプトをダウンロード＆実行
curl -fsSL https://raw.githubusercontent.com/yourname/ClaudeSkills/main/install.sh | bash -s laravel
```

#### Option 3: 手動コピー

```bash
# テンプレートリポジトリをローカルにクローン
git clone https://github.com/yourname/ClaudeSkills ~/ClaudeSkills

# 各プロジェクトで
cd your-laravel-project
cp -r ~/ClaudeSkills/templates/laravel/.claude .
cp ~/ClaudeSkills/templates/laravel/CLAUDE.md.template CLAUDE.md
```

### 4.3 install.shの実装例

```bash
#!/bin/bash
# templates/laravel/install.sh

set -e

echo "=== Laravel TDD Skills Installer ==="

# 引数チェック
if [ -z "$1" ]; then
    TARGET_DIR="."
else
    TARGET_DIR="$1"
fi

cd "$TARGET_DIR"

# 1. 既存チェック
if [ -d ".claude/skills/tdd-init" ]; then
    echo "⚠️  TDD Skills already installed."
    read -p "Overwrite? (y/N): " -n 1 -r
    echo
    if [[ ! $REPLY =~ ^[Yy]$ ]]; then
        echo "Cancelled."
        exit 0
    fi
fi

# 2. ディレクトリ作成
echo "📁 Creating directories..."
mkdir -p .claude/skills
mkdir -p .claude/commands
mkdir -p docs/tdd

# 3. Skillsをコピー
echo "📦 Installing Skills..."
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cp -r "$SCRIPT_DIR/.claude/skills/"* .claude/skills/
cp -r "$SCRIPT_DIR/.claude/commands/"* .claude/commands/

# 4. CLAUDE.mdを生成
if [ ! -f "CLAUDE.md" ]; then
    echo "📝 Creating CLAUDE.md..."
    cp "$SCRIPT_DIR/CLAUDE.md.template" CLAUDE.md
else
    echo "ℹ️  CLAUDE.md already exists, skipping."
fi

# 5. .gitignore更新
if [ -f ".gitignore" ]; then
    if ! grep -q "CLAUDE.local.md" .gitignore; then
        echo "" >> .gitignore
        echo "# Claude Code personal settings" >> .gitignore
        echo "CLAUDE.local.md" >> .gitignore
        echo ".claude/local/" >> .gitignore
    fi
fi

# 6. プロジェクト固有の設定を検出
echo "🔍 Detecting project configuration..."
if [ -f "composer.json" ]; then
    # Pest or PHPUnit?
    if grep -q "pestphp/pest" composer.json; then
        echo "✅ Detected: Pest"
        # Pest用の設定を有効化
    else
        echo "✅ Detected: PHPUnit"
    fi

    # PHPStan?
    if grep -q "phpstan/phpstan" composer.json; then
        echo "✅ Detected: PHPStan"
    fi
fi

echo ""
echo "✅ Installation complete!"
echo ""
echo "📚 Next steps:"
echo "1. Review: cat CLAUDE.md"
echo "2. Customize: vim CLAUDE.md (if needed)"
echo "3. Start TDD: /tdd-init 'Your feature description'"
echo ""
echo "🔗 Documentation: https://github.com/yourname/ClaudeSkills"
```

---

## 5. グローバルインストールを使うケース

### 5.1 グローバルが適切な場合

```bash
~/.claude/skills/
├── personal-shortcuts/     # 個人的なショートカット
├── code-review-checklist/  # 汎用的なレビューリスト
└── git-workflow/           # Git操作のヘルパー
```

**特徴**:
- フレームワーク非依存
- 全プロジェクトで同じ動作
- 実験的・個人的なSkills

### 5.2 プロジェクト固有が適切な場合（TDD Skills）

```bash
project/.claude/skills/
├── tdd-init/        # プロジェクト固有のルール
├── tdd-plan/        # フレームワーク固有のテンプレート
├── tdd-red/         # フレームワーク固有のテスト構文
└── laravel-quality/ # Laravel固有の品質チェック
```

**特徴**:
- フレームワーク依存
- プロジェクト固有のルール
- チーム共有の可能性

### 5.3 ハイブリッド例（参考）

将来的に検討する場合:

```bash
# グローバル: TDDの基本概念
~/.claude/skills/
├── tdd-concepts/         # TDDの一般的な概念
└── refactoring-patterns/ # 汎用的なリファクタパターン

# プロジェクト: フレームワーク固有
project/.claude/skills/
├── tdd-plan/             # Laravel固有のプランニング
├── tdd-red/              # Pest固有のテスト記法
└── laravel-quality/      # Laravel固有の品質基準
```

---

## 6. メンテナンス戦略

### 6.1 更新の流れ

```
1. ClaudeSkillsリポジトリで改善
   ↓
2. templates/laravel/ を更新
   ↓
3. バージョンタグを付ける (v1.0.0)
   ↓
4. 必要なプロジェクトで再インストール
   cd your-laravel-project
   curl -fsSL ... | bash -s laravel
   ↓
5. プロジェクト固有の調整
```

### 6.2 バージョン管理

```bash
# templates/laravel/.claude/VERSION
1.0.0

# プロジェクトで確認
cat .claude/VERSION

# アップグレード時に差分確認
git diff .claude/
```

### 6.3 カスタマイズの管理

各プロジェクトでカスタマイズした場合:

```bash
your-laravel-project/.claude/
├── skills/
│   ├── tdd-plan/
│   │   └── SKILL.md  # ← カスタマイズ済み
│   └── CUSTOMIZATIONS.md  # ← カスタマイズ内容を記録
```

---

## 7. まとめ

### 推奨戦略: **プロジェクト固有 + テンプレートリポジトリ**

| 要素 | 決定 |
|------|------|
| **メイン配置** | `.claude/skills/` (プロジェクト固有) |
| **配布方法** | テンプレートリポジトリ + install.sh |
| **フレームワーク対応** | templates/laravel/, templates/django/, ... |
| **グローバル** | 使わない（またはプロジェクト非依存のSkillsのみ） |

### 理由

1. ✅ フレームワークごとにカスタマイズ可能
2. ✅ プロジェクトごとの要件に対応
3. ✅ gitで管理・共有可能
4. ✅ 各プロジェクトが独立して進化可能
5. ✅ 必要なプロジェクトだけ最新版に更新

### 次のアクション

1. **ClaudeSkillsリポジトリの構造を作成**
   ```bash
   mkdir -p templates/laravel/.claude/skills
   ```

2. **1つのSkillを作成（tdd-plan）**
   ```bash
   templates/laravel/.claude/skills/tdd-plan/SKILL.md
   ```

3. **install.shを作成**
   ```bash
   templates/laravel/install.sh
   ```

4. **既存のLaravelプロジェクトでテスト**
   ```bash
   cd your-existing-laravel-project
   ./install.sh
   ```

---

*作成日: 2025-10-22*
*推奨: プロジェクト固有インストール + テンプレートリポジトリ*
