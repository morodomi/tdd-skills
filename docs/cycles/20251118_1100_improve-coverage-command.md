---
feature: commands
cycle: improve-coverage-command-001
phase: DONE
created: 2025-11-18 11:00
updated: 2025-11-18 11:30
---

# カバレッジ向上用Slash Command作成

## やりたいこと

TDDサイクル完了後、ラインカバレッジが下がってきたと感じた時に手動実行する `/improve-coverage` コマンドを作成する。

**目的**:
- テストカバレッジを自動的に向上させる（現在値+10%、上限90%）
- 既存コードは触らず、テスト追加のみで対応
- 成功時はPR作成、失敗時はIssue作成で問題を報告

**実行例**:
```bash
# TDDサイクル完了後、カバレッジをチェック
php artisan test --coverage

# カバレッジが下がっていたら実行
/improve-coverage
```

---

## Scope Definition

### 実装範囲

1. **Slash Command作成**: `.claude/commands/improve-coverage.md`
2. **カバレッジ測定**: PHPUnit `--coverage-text` でカバレッジ取得
3. **対象ファイル選定**: 直近更新頻度 + カバレッジ低い + コアロジック優先
4. **ブランチ作成**: `test/improve-coverage-YYYYMMDD-HHmm`
5. **テスト追加**:
   - UnitとFeatureの両方
   - 既存テスト構造に合わせる（500行超えたらディレクトリ分割）
   - 既存コードは触らない
6. **PHPStan対応**: Level 8まで段階的に上げる（テストコードのみ）
7. **カバレッジ再測定**: +5%〜10%達成確認
8. **PR作成**: 成功時は `gh pr create` で自動作成
9. **Issue作成**: 失敗時は `gh issue create` で問題報告

### 非実装範囲

1. **既存コードのリファクタ**: 必要な場合はIssue作成で報告
2. **カバレッジ以外の品質指標**: 静的解析は実施するが、品質向上は別途
3. **CI統合**: 手動実行のみ、自動実行は将来対応
4. **複数フレームワーク対応**: Laravel専用（将来的に拡張）

### 変更予定ファイル

1. `.claude/commands/improve-coverage.md` (新規作成)
2. `tests/improve-coverage-command/test_command_structure.sh` (新規作成)
3. `tests/improve-coverage-command/test_execution_flow.sh` (新規作成)
4. `tests/improve-coverage-command/run_all_tests.sh` (新規作成)

---

## Context & Dependencies

### 参照ドキュメント・リソース

1. **既存コマンド**: `.claude/commands/code-review.md`（並行Subagent実行の参考）
2. **PHPUnit Coverage**: `vendor/bin/phpunit --coverage-text`
3. **gh CLI**: Issue/PR作成（`gh issue create`, `gh pr create`）
4. **git**: ブランチ作成、コミット、プッシュ
5. **要件まとめ**: 前述の要件定義

### 依存する機能・ツール

1. **Task tool**: general-purposeエージェントを呼び出し
2. **Bash tool**: git, gh, phpunit等のコマンド実行
3. **Read/Grep tool**: カバレッジレポート解析、既存テスト構造確認
4. **Edit/Write tool**: テストコード作成（Agentが実行）

### 技術的な制約

1. **PHPUnitのカバレッジ**: Xdebug or PCOV必須
2. **gh CLI**: 認証済みであること
3. **git**: リモートリポジトリへのpush権限
4. **Laravel**: composer.jsonに`laravel/framework`が存在

---

## Test List

### 実装予定（TODO）

#### コマンド構造テスト（test_command_structure.sh）

- [ ] TC-01: `.claude/commands/improve-coverage.md`が存在する
- [ ] TC-02: 有効なYAML frontmatterを持つ（description等）
- [ ] TC-03: Task tool使用の指示がある（`Task` toolで`general-purpose`エージェント起動）
- [ ] TC-04: カバレッジ測定の指示がある（`phpunit --coverage-text`）
- [ ] TC-05: 対象ファイル選定の指示がある（git log, 直近更新頻度）
- [ ] TC-06: ブランチ作成の指示がある（`test/improve-coverage-YYYYMMDD-HHmm`）
- [ ] TC-07: テスト追加の指示がある（既存構造に合わせる、500行制限）
- [ ] TC-08: PHPStan対応の指示がある（Level 8まで段階的）
- [ ] TC-09: PR作成の指示がある（成功時、`gh pr create`）
- [ ] TC-10: Issue作成の指示がある（失敗時、`gh issue create`）
- [ ] TC-11: エラーハンドリングの指示がある（ユーザー確認、既存コード不変）

#### 実行フローテスト（test_execution_flow.sh）

- [ ] TC-12: カバレッジレポート解析ロジックが記述されている
- [ ] TC-13: コアロジック優先判定の指示がある（Service, Controller等）
- [ ] TC-14: 既存テスト構造確認の指示がある（Read/Grep tool使用）
- [ ] TC-15: テスト追加後のカバレッジ再測定指示がある
- [ ] TC-16: 成功判定条件が明確（+5%〜10%、上限90%）
- [ ] TC-17: PRスコープ制限の指示がある（10ファイル未満）

### 実装中（WIP）

（なし）

### 実装中に気づいた追加テスト（DISCOVERED）

（なし）

### 完了（DONE）

（なし）

---

## Implementation Notes

### 背景

ClaudeSkillsプロジェクトでは、厳格なTDDワークフローを実現するために、各フェーズごとにSkillsやCommandsを作成している。

しかし、**TDDサイクル完了後にカバレッジが下がる問題**がある:
- 新機能追加時、既存コードのカバレッジが相対的に下がる
- リファクタ時、削除したコードのテストが残り、実質的なカバレッジが低下
- 手動でテスト追加するのは時間がかかり、優先順位が下がりがち

この問題を解決するため、**カバレッジ向上を自動化するSlash Command**を作成する。

### 設計方針

#### 1. Task Agent型

Slash Commandから`Task` toolを使ってgeneral-purposeエージェントを起動し、完全自律で実行する。

```markdown
## 実行フロー

1. ユーザーが `/improve-coverage` を実行
2. Slash Commandがカバレッジ測定
3. Task Agent起動:
   - 対象ファイル選定
   - ブランチ作成
   - テスト追加
   - PHPStan対応
   - カバレッジ再測定
   - 成功判定 → PR作成 or Issue作成
4. 結果をユーザーに報告
```

#### 2. 既存コードは触らない哲学

**重要**: 既存コードのバグやリファクタが必要な場合は、**gh issueでチケット作成**する。

理由:
- Subagentが既存コードを変更すると、予期せぬ副作用のリスク
- カバレッジ向上とリファクタは別のサイクルで実施すべき
- Issueで優先度を設定し、後で対応

#### 3. 逆TDDアプローチ

通常のTDD: RED（テスト） → GREEN（実装） → REFACTOR

カバレッジ向上: 既存コード（GREEN状態） → テスト追加（カバレッジ測定） → 検証

フェーズ定義:
- **INIT**: カバレッジ測定、対象ファイル選定
- **PLAN**: テスト追加計画
- **IMPLEMENT**: テスト追加
- **VERIFY**: カバレッジ再測定、目標達成確認
- **COMMIT**: PR作成 or Issue作成

#### 4. コアロジック優先判定

優先順位:
1. **直近更新頻度が多い**（`git log --since="1 month ago" --name-only`）
2. **カバレッジが低い**（未カバー行数が多い）
3. **Laravel固有**: Service, Controller等の命名（該当する場合のみ）

例:
```bash
# 直近1ヶ月の更新頻度を取得
git log --since="1 month ago" --name-only --pretty=format: | \
  grep ".php$" | sort | uniq -c | sort -rn | head -10

# カバレッジレポートと照合
phpunit --coverage-text | grep "app/"
```

#### 5. テスト構造の維持

既存テスト構造に合わせる:
- `tests/Unit/UserServiceTest.php` → 新しいテストメソッド追加
- 500行超える場合: `tests/Unit/UserService/LoginTest.php` とディレクトリ分割

#### 6. 失敗時のハンドリング

| 状況 | 対応 |
|------|------|
| テスト全通過 + カバレッジ上昇 | ✅ Commit & Push & PR作成 |
| テストが失敗する | ❌ ユーザーに確認 → Issue作成 |
| 既存コードにバグ発見 | ❌ Issue作成（priority設定） |
| PHPStan Level 8で解決不可 | ⚠️ ユーザーに確認 |
| カバレッジが上がらない | ⚠️ ユーザーに確認、別アプローチ提案 |

#### 7. Issue作成フォーマット

```markdown
title: "[coverage] UserService: テスト追加時にバグ発見"
labels: bug, coverage, priority:high
body:
  ## 概要
  カバレッジ向上作業中に既存コードのバグを発見しました。

  ## 詳細
  - **ファイル**: `app/Services/UserService.php`
  - **メソッド**: `updateProfile()`
  - **問題**: null チェックが不足
  - **再現手順**: ...
  - **期待する動作**: ...
  - **実際の動作**: ...

  ## 優先度
  - **High**: ユーザーデータ破損の可能性

  ## 関連ブランチ
  - `test/improve-coverage-20251118-1100`
```

#### 8. PR作成フォーマット

```markdown
title: "test: カバレッジ向上 - UserService (65% → 78%)"
body:
  ## 概要
  UserServiceのカバレッジを65%から78%に向上しました（+13%）。

  ## 変更内容
  - `tests/Unit/UserServiceTest.php`: 新規テスト5件追加
  - `tests/Unit/UserService/LoginTest.php`: ディレクトリ分割（500行超過）

  ## カバレッジ詳細
  | ファイル | Before | After | Diff |
  |---------|--------|-------|------|
  | UserService.php | 65% | 78% | +13% |

  ## テスト結果
  ```
  Tests: 47 passed
  Coverage: 78.3%
  ```

  ## 静的解析
  - PHPStan: Level 8 (0 errors)
  - Laravel Pint: OK

  🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

---

## 詳細設計

### 1. コマンド構造

`.claude/commands/improve-coverage.md` の構造:

```markdown
---
description: カバレッジ向上を自動化。テスト追加 + PHPStan対応 + PR/Issue作成
---

# Improve Coverage Command

TDDサイクル完了後、ラインカバレッジを自動的に向上させます。

## 使用方法

\`\`\`bash
/improve-coverage
\`\`\`

## 実行フロー

### Step 1: カバレッジ測定

Bashツールを使ってPHPUnitでカバレッジを測定:

\`\`\`bash
vendor/bin/phpunit --coverage-text --colors=never > /tmp/coverage_before.txt
\`\`\`

カバレッジレポートを解析し、現在の全体カバレッジ率を取得。

### Step 2: 対象ファイル選定

Taskツールでgeneral-purposeエージェントを起動し、以下を実行:

1. **直近更新頻度を取得**:
   \`\`\`bash
   git log --since="1 month ago" --name-only --pretty=format: | \
     grep "\.php$" | grep -E "app/(Services|Http/Controllers)" | \
     sort | uniq -c | sort -rn
   \`\`\`

2. **カバレッジレポートと照合**:
   - カバレッジが低い（70%未満）
   - 更新頻度が高い（直近1ヶ月で5回以上）
   - コアロジック（Service, Controller等）

3. **優先順位付け**: スコア = (100 - カバレッジ%) × 更新頻度

4. **PRスコープ制限**: 上位から選択し、10ファイル未満に制限

### Step 3: Task Agent起動

選定したファイルリストをもとに、Taskツールで以下を実行:

\`\`\`
Task tool (general-purpose agent):
  - ブランチ作成: test/improve-coverage-YYYYMMDD-HHmm
  - 各ファイルについて:
    - 既存テスト構造確認（Read/Grep）
    - テスト追加（既存構造に合わせる、500行制限）
    - PHPStan Level 8対応（段階的）
  - カバレッジ再測定
  - 成功判定:
    - テスト全通過 + カバレッジ+5%以上 → PR作成
    - テスト失敗 or バグ発見 → Issue作成
\`\`\`

### Step 4: 結果報告

ユーザーに以下を報告:
- カバレッジ変化（Before/After）
- 追加したテスト数
- PR URLまたはIssue URL
```

### 2. カバレッジレポート解析

PHPUnitの`--coverage-text`出力例:

```
Code Coverage Report:
  2025-11-18 11:00:00

 Summary:
  Classes: 85.00% (17/20)
  Methods: 78.26% (18/23)
  Lines:   72.41% (210/290)

app/Services/UserService.php
  Methods:  60.00% ( 3/ 5)
  Lines:    65.22% ( 15/ 23)
```

解析ロジック:
1. "Lines:" 行から全体カバレッジ率を取得（例: 72.41%）
2. 各ファイルのカバレッジ率を取得（例: UserService.php = 65.22%）
3. 目標値を計算: min(現在値 + 10%, 90%)

### 3. 対象ファイル選定アルゴリズム

```python
# 疑似コード
files = []
for file in git_log_files:
    if not matches_core_logic(file):  # Service, Controller等
        continue

    coverage = get_coverage(file)
    frequency = get_update_frequency(file)

    if coverage < 70 and frequency >= 5:
        score = (100 - coverage) * frequency
        files.append((file, score, coverage, frequency))

# スコア降順でソート
files.sort(key=lambda x: x[1], reverse=True)

# 10ファイル未満に制限
selected = files[:10]

# カバレッジ目標: +5%〜10%、上限90%
for file, score, coverage, freq in selected:
    target = min(coverage + 10, 90)
    print(f"{file}: {coverage}% → {target}%")
```

### 4. テスト追加戦略

#### 4.1 既存テスト構造確認

```bash
# 既存テストファイルを検索
find tests -name "*UserService*Test.php"

# 既存テストの行数を確認
wc -l tests/Unit/UserServiceTest.php
```

#### 4.2 テスト追加パターン

**パターンA: 既存テストファイルに追加**（500行未満の場合）

```php
// tests/Unit/UserServiceTest.php

#[Test]
public function ユーザープロフィール更新が成功する(): void
{
    // Given: ユーザーが存在する
    $user = User::factory()->create();

    // When: プロフィールを更新する
    $result = $this->service->updateProfile($user->id, ['name' => 'New Name']);

    // Then: 更新される
    $this->assertTrue($result);
    $this->assertEquals('New Name', $user->fresh()->name);
}
```

**パターンB: ディレクトリ分割**（500行超える場合）

```bash
# 既存ファイル移動
mkdir -p tests/Unit/UserService
mv tests/Unit/UserServiceTest.php tests/Unit/UserService/BaseTest.php

# 新規ファイル作成
# tests/Unit/UserService/LoginTest.php
# tests/Unit/UserService/ProfileTest.php
```

#### 4.3 PHPStan対応（段階的）

```bash
# Level 0から開始
vendor/bin/phpstan analyse tests/Unit/UserServiceTest.php --level 0

# エラーがなければLevel 1へ
vendor/bin/phpstan analyse tests/Unit/UserServiceTest.php --level 1

# ... Level 8まで繰り返す
```

### 5. 成功判定フロー

```
カバレッジ再測定
  ↓
テスト全通過?
  ├─ NO → Issue作成（テスト失敗）
  └─ YES → カバレッジ上昇?
             ├─ NO → Issue作成（カバレッジ上がらず）
             └─ YES → +5%以上?
                       ├─ NO → Issue作成（目標未達）
                       └─ YES → PR作成（成功）
```

### 6. Issue作成テンプレート

```markdown
title: "[coverage] {FileName}: テスト追加時に{問題内容}"
labels: coverage, {priority}, {type}
body:
  ## 概要
  カバレッジ向上作業中に{問題内容}が発生しました。

  ## 詳細
  - **ファイル**: `{file_path}`
  - **現在のカバレッジ**: {coverage}%
  - **目標カバレッジ**: {target}%
  - **問題**: {description}

  ## 再現手順
  1. ...
  2. ...

  ## 優先度
  - **{priority}**: {reason}

  ## 関連ブランチ
  - `test/improve-coverage-{timestamp}`

  ## 推奨対応
  {recommendation}
```

### 7. PR作成テンプレート

```markdown
title: "test: カバレッジ向上 - {FileNames} ({before}% → {after}%)"
body:
  ## 概要
  {N}ファイルのカバレッジを{before}%から{after}%に向上しました（+{diff}%）。

  ## 変更内容
  {changes_summary}

  ## カバレッジ詳細
  | ファイル | Before | After | Diff |
  |---------|--------|-------|------|
  {coverage_table}

  ## テスト結果
  ```
  Tests: {passed} passed
  Coverage: {coverage}%
  PHPStan: Level 8 (0 errors)
  ```

  ## 静的解析
  - PHPStan: Level 8 ✓
  - Laravel Pint: OK ✓

  🤖 Generated with [Claude Code](https://claude.com/claude-code)

  Co-Authored-By: Claude <noreply@anthropic.com>
```

---

## Progress Log

### 2025-11-18 11:00 - INIT phase
- Cycle doc作成
- やりたいことを定義
- Scope Definition記入（実装範囲9項目、非実装範囲4項目、変更予定4ファイル）
- Context & Dependencies記入（参照ドキュメント5件、依存機能4件、技術的制約4件）
- Implementation Notes記入（背景、設計方針8項目）

### 2025-11-18 11:05 - PLAN phase
- Test List作成完了（TODO: 17件）
  - コマンド構造テスト: TC-01〜TC-11（11件）
  - 実行フローテスト: TC-12〜TC-17（6件）
- 詳細設計記入完了:
  - 1. コマンド構造（4ステップ実行フロー）
  - 2. カバレッジレポート解析（PHPUnit --coverage-text）
  - 3. 対象ファイル選定アルゴリズム（スコアリング方式）
  - 4. テスト追加戦略（既存構造維持、500行制限、PHPStan段階的対応）
  - 5. 成功判定フロー（テスト通過 + カバレッジ+5%以上）
  - 6. Issue作成テンプレート
  - 7. PR作成テンプレート
- YAML frontmatter更新（phase: INIT → PLAN）

### 2025-11-18 11:10 - RED phase
- テストスクリプト作成完了:
  - `tests/improve-coverage-command/test_command_structure.sh` (TC-01〜TC-11)
  - `tests/improve-coverage-command/test_execution_flow.sh` (TC-12〜TC-17)
  - `tests/improve-coverage-command/run_all_tests.sh`
- テスト実行: 全17テスト失敗確認（実装なし）
- YAML frontmatter更新（phase: RED → GREEN）

### 2025-11-18 11:15 - GREEN phase
- `.claude/commands/improve-coverage.md` 作成（15,540 bytes）
- install.sh更新（3ファイル: Laravel, Bedrock, Generic）
- コマンド名変更: `improve-coverage` → `test-agent`
  - ファイルリネーム: `.claude/commands/test-agent.md`
  - テストディレクトリリネーム: `tests/test-agent-command/`
  - sedで一括置換（テストスクリプト、install.sh）
- テスト実行: 全17テスト成功
- YAML frontmatter更新（phase: GREEN → REFACTOR）

### 2025-11-18 11:25 - REFACTOR phase
- `.claude/commands/test-agent.md`内の旧命名修正:
  - `test/improve-coverage` → `test/test-agent`（6箇所）
  - `/improve-coverage` → `/test-agent`
- grep確認: 旧命名0件
- テスト再実行: 全17テスト成功
- YAML frontmatter更新（phase: REFACTOR → REVIEW）

### 2025-11-18 11:30 - REVIEW phase
- `/code-review` コマンド実行（並行3 Subagent）:
  - **Correctness Review** (haiku): 0 issues
    - ロジックエラー: なし
    - エッジケース: 適切に処理
    - 要件準拠: 満たしている
    - コード一貫性: 高（code-review.mdパターンと一致）
  - **Performance Review** (haiku + Gemini): 0 issues
    - アルゴリズム効率: O(1)
    - ファイルI/O: 最小限（1ファイルコピー）
    - スクリプト実行: 影響なし
  - **Security Review** (haiku): 0 issues
    - 入力検証: N/A（固定パス）
    - SQLインジェクション: N/A
    - XSS: N/A
    - 秘密情報漏洩: なし
- **総合評価**: Critical 0件、Important 0件、Optional 0件
- **結論**: マージ可能な品質
- YAML frontmatter更新（phase: REVIEW → DONE）

（次のフェーズ: COMMIT - コミット + PR作成）
