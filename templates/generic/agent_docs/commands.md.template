# ${PROJECT_NAME} Commands

## テスト

```bash
# すべてのテスト実行
${TEST_COMMAND}

# 特定のテストファイル
${TEST_FILE_COMMAND}

# 失敗時に停止
${TEST_STOP_COMMAND}
```

## カバレッジ

```bash
# カバレッジレポート
${COVERAGE_COMMAND}

# 最低カバレッジを強制
${COVERAGE_MIN_COMMAND}
```

## 静的解析

```bash
# 解析実行
${STATIC_ANALYSIS_COMMAND}
```

## コードフォーマット

```bash
# 自動修正
${FORMATTER_COMMAND}

# チェックのみ
${FORMATTER_CHECK_COMMAND}
```

## パッケージ管理

```bash
# 依存関係インストール
${INSTALL_COMMAND}

# 依存関係更新
${UPDATE_COMMAND}
```

## 一括品質チェック

```bash
# すべてのチェックを実行
${CHECK_ALL_COMMAND}
```
