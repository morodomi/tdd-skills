# ${PROJECT_NAME} Quality Standards

## テストカバレッジ

| 指標 | 値 |
|------|-----|
| 目標 | 90%以上 |
| 最低ライン | 80% |

### 測定コマンド

```bash
${COVERAGE_COMMAND}
```

---

## 静的解析

| 指標 | 値 |
|------|-----|
| ツール | ${STATIC_ANALYSIS} |
| エラー許容 | 0件 |

### 実行コマンド

```bash
${STATIC_ANALYSIS_COMMAND}
```

---

## コードフォーマット

| 指標 | 値 |
|------|-----|
| ツール | ${FORMATTER} |
| 規約 | ${CODE_STYLE} |

### 実行コマンド

```bash
# フォーマット実行
${FORMATTER_COMMAND}

# チェックのみ
${FORMATTER_CHECK_COMMAND}
```

---

## 品質チェックの実行順序

```bash
# 1. テスト実行
${TEST_COMMAND}

# 2. カバレッジ確認
${COVERAGE_COMMAND}

# 3. 静的解析
${STATIC_ANALYSIS_COMMAND}

# 4. フォーマットチェック
${FORMATTER_CHECK_COMMAND}
```

---

*すべての品質基準を満たしてからコミットしてください*
