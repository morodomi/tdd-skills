# [機能名] 実装計画

## ユーザーストーリー

**As a** [ユーザー種別]
**I want to** [実現したいこと]
**So that** [目的・価値]

## 受け入れ基準

### シナリオ1: [シナリオ名]
**Given** [前提条件]
**When** [アクション]
**Then** [期待結果]
**And** [追加の期待結果]

### シナリオ2: [シナリオ名]
**Given** [前提条件]
**When** [アクション]
**Then** [期待結果]
**And** [追加の期待結果]

## 実装範囲

### 対象ファイル
- `[path/to/file]` - [説明]
- `[path/to/another/file]` - [説明]

### 対象外
- [この機能で実装しないもの]
- [後のフェーズで実装するもの]

## アーキテクチャ設計

### データ構造（必要に応じて）

プロジェクトに応じてデータ構造を記述してください。

**例（クラスベース）**:
```
class XXX {
    // フィールド
    - field1
    - field2

    // メソッド
    + method1()
    + method2()
}
```

**例（データベース）**:
```
table: xxx
- id (primary key)
- field1
- field2
- created_at
- updated_at
```

### API設計（必要に応じて）

**エンドポイント**: `[METHOD] /api/[resource]`
**パラメータ**:
- `param1` ([type], required) - 説明
- `param2` ([type], optional) - 説明

**レスポンス**:
```json
{
    "data": {
        "id": 1,
        "field1": "value1",
        "field2": "value2"
    }
}
```

### UI/UX設計（必要に応じて）

**画面名**: XXX一覧画面

**レイアウト**:
```
+---------------------------+
| ヘッダー                   |
+---------------------------+
| メインコンテンツ           |
+---------------------------+
| フッター                   |
+---------------------------+
```

**インタラクション**:
- ユーザーアクション → システム応答
- フォーム送信 → バリデーション → 処理

## テストケース一覧

### [Your Test Type 1] Tests

1. `[path/to/test/file]`
   - テストケース1の説明
   - テストケース2の説明
   - テストケース3の説明

2. `[path/to/another/test/file]`
   - テストケース1の説明
   - テストケース2の説明

### [Your Test Type 2] Tests

1. `[path/to/unit/test/file]`
   - テストケース1の説明
   - テストケース2の説明
   - テストケース3の説明

## 見積もり

- テスト作成: [時間]
- 実装: [時間]
- リファクタリング: [時間]

**合計**: 約[時間]

## 依存関係

- なし

または

- XXX機能が実装済みであること
- YYYが存在すること

## リスク

### リスク1: [リスク名]
**発生条件**: [どんな時に発生するか]
**影響**: [どんな影響があるか]
**対策**: [どう対処するか]

### リスク2: [リスク名]
**発生条件**: [どんな時に発生するか]
**影響**: [どんな影響があるか]
**対策**: [どう対処するか]

## 品質基準

### テストカバレッジ
- 目標: 90%以上
- 測定方法: `[your coverage command]`
- 最低ライン: 80%

### 静的解析
- ツール: [your static analysis tool]
- レベル: [最高レベル]
- 実行コマンド: `[your static analysis command]`
- エラー許容: 0件

### コード品質
- TDD徹底: すべての機能にテスト必須
- Given/When/Thenコメントで意図を明確化
- テストフレームワークの規約に従う

---

## プロジェクト固有のカスタマイズ

このテンプレートは汎用的な内容です。
プロジェクト固有の要件に応じてセクションを追加・削除してください。

### 追加可能なセクション例

**データベース設計**（関係データベース使用時）:
```
### データベース設計

**テーブル名**: xxx

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|-----|------|----------|------|
| id | int | NO | - | 主キー |
| field1 | varchar(255) | NO | - | 説明 |
| created_at | timestamp | YES | NULL | 作成日時 |

**インデックス**:
- PRIMARY KEY (id)
- INDEX idx_field1 (field1)
```

**フロントエンド設計**（SPAアプリケーション）:
```
### コンポーネント設計

**コンポーネント名**: XXXComponent

**Props**:
- prop1: string - 説明
- prop2: number - 説明

**State**:
- state1: boolean - 説明
- state2: array - 説明

**Events**:
- onAction1: () => void - 説明
- onAction2: (data) => void - 説明
```

**パフォーマンス要件**:
```
### パフォーマンス要件

- レスポンスタイム: 200ms以内
- 同時接続数: 1000ユーザー
- データ量: 100万件まで対応
```

**セキュリティ要件**:
```
### セキュリティ要件

- 認証: [認証方式]
- 認可: [認可方式]
- 入力検証: すべての入力をサニタイズ
- 出力エスケープ: すべての出力をエスケープ
```

---

*作成日時: YYYY-MM-DD HH:MM*
*次のフェーズ: RED（テスト作成）*
